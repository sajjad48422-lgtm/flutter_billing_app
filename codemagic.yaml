workflows:
  debug-apk:
    name: Debug APK

    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build apk --debug

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
